


# Averages for results comparison
meanRun <- numeric(nopt)
sdRun <- numeric(nopt)
meanS <- numeric(nopt)
sdS <- numeric(nopt)
meanH <- numeric(nopt)
sdH <- numeric(nopt)
meanSmsy <- numeric(nopt)
sdSmsy <- numeric(nopt)
meanSmsyBias <- numeric(nopt)
sdSmsyBias <- numeric(nopt)
meanU <- numeric(nopt)
meanUImp <- numeric(nopt)
pctOF <- numeric(nopt)
sdpctOF <- numeric(nopt)
pctEX <- numeric(nopt)
sdpctEX <- numeric(nopt)



# Summary list for model parameters

