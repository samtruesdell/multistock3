


# Averages for results comparison
meanRun <- numeric(nopt)
meanS <- numeric(nopt)
meanH <- numeric(nopt)
meanSmsy <- numeric(nopt)
meanSmsyBias <- numeric(nopt)
meanU <- numeric(nopt)
meanUImp <- numeric(nopt)
pctOF <- numeric(nopt)
pctEX <- numeric(nopt)



# Summary list for model parameters

